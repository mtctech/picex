import React, { PropsWithChildren } from 'react';

/**
 * 主区域画布
 * @description
 * 1. 根据配置初始化并渲染画布
 * 2. 根据Blocks树渲染画布内容
 */
export function PicexCanvas(props: PropsWithChildren<{}>) {
	return <div className=""></div>;
}
